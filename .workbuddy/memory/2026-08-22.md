# 2026-08-22

## 个人简历网站改版（Neo-Brutalism / 波普硬核新丑风）
- 文件：`D:\Vibecodimg\网站\index.html`（单文件，原 JS `resume` 数据对象文案/链接全部保留）
- 技术栈：Tailwind Play CDN（布局）+ Remix Icon + Space Grotesk(正文)/Archivo Black(超粗标题)
- 设计令牌：CSS 变量 `--c-bg:#FFFDF0` 全局底色 + `radial-gradient` 20px 纯黑波点；碰撞色 mint `#A3E635`/violet `#C084FC`/yellow `#FDE047`/sky `#38BDF8`
- 核心原语：`.brutal-card`(3px 黑边 + 4px 4px 0 硬阴影，hover 反向位移 -4,-4 加深至 8px)；`.brutal-btn`(hover 右下移 2px 阴影缩 2px，active 贴合归零)；`.brutal-badge` 色块徽章随机旋转 -2°~2.5°（JS 注入）
- 细节：导航/返回顶部/贴纸 sticker 统一硬阴影；移动端 `overflow-x:hidden` 防硬阴影溢出；保留深浅色切换（brutalist 反转变体）与打印样式
- 预览：本地 `python -m http.server 8123`（127.0.0.1:8123/index.html）

## 落地页 landing.html（Neo-Brutalism 同视觉语言）
- 新建 `D:\Vibecodimg\网站\landing.html`：个人简历网站兼作品集展示落地页（不覆盖 index.html）
- 结构：导航 → Hero(强 CTA：查看完整简历/联系我) → 价值主张数据条 → 关于我 → 精选作品 → 能力矩阵 → 证书与成就 → 最终 CTA(联系) → 页脚
- 复用同一套 design tokens / 硬阴影原语 / 碰撞色 / 贴纸随机旋转；保留深浅色、打印、返回顶部、邮箱复制；端口 8123 同服可预览

## 融合：统一主页 index.html
- 将 landing.html 视觉基调作为基底，把 index.html 的简历核心内容合并进同一风格框架，产出统一 `index.html`（原为简历站，现为「落地页风格个人简历主页」）
- 合并区块顺序：Hero → 亮点成果 → 关于我 → 实践经历(Tabs) → 项目经历(Tabs, 空占位) → 能力矩阵 → 精选作品 → 教育与证书(edu 卡 + cert 卡 + 证书图片画廊 + 自我评价) → 最终 CTA(联系) → 页脚
- 新增来自简历站的内容：实践经历 Tabs、项目经历 Tabs、教育背景卡、证书图片画廊(高级/初级/初赛/广东省决赛 jpg+webp + 点击放大模态框)、自我评价；Hero 增加联系方式条(电话/邮箱复制/微信/定位)
- 所有文案/数据/链接完整保留；landing.html 已于 00:58 经用户确认删除，现仅保留统一主页 index.html
